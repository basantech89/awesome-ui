import { BrandColor, CommonSize } from '../types'

export const brandColors: BrandColor[] = [
  'primary',
  'secondary',
  'tertiary',
  'success',
  'info',
  'warning',
  'error'
]

export const commonSizes: CommonSize[] = ['xs', 'sm', 'md', 'lg']
